# Custom Building / 自定义建筑

**只用数据包，就能造出属于你自己的建筑蓝图物品。** 一个 json 文件描述结构、预览图、物品贴图、名称和说明文字——重载数据包之后，蓝图就会出现在专属的「自定义建筑」创造选项卡里，随时可以放进世界。

不需要资源包，不需要模型文件，不需要写代码：会写数据包，就会加建筑。

---

## 功能

* **完全数据驱动的蓝图**——结构、预览图、物品贴图、显示名称、tooltip 全部来自
  `data/<命名空间>/custom_building/blueprint/` 下的一个文件。
* **`/reload` 后立即生效**——不用重启游戏，不用资源包。
* **像样的建造面板**，抄自 MC-Prefab：左边控制面板、右边预览图，底部是 预览 / 取消 / 建造。
* **先定朝向再造**——循环 北/东/南/西，面板会记住你上次用的朝向。
* **半透明幽灵预览**，用的是 Litematica 那套渲染手法：按 16³ 区块缓存顶点缓冲、全亮无光照烘焙、
  以 0.5 alpha 走半透明方块层绘制。**看到的就是放下的**。
  * **左键** 取消预览；
  * **右键** 就地建造；
  * **潜行 + 滚轮** 原地转动朝向。
* **说人话的失败提示**（黄色，措辞仿 MC-Prefab）：受保护方块、不可破坏方块、出生点保护、结构文件缺失；
  成功则是绿色提示加音效。
* **贴图路径很宽容**——同一个路径五种写法都能用，缺 `textures/`、缺 `.png` 会自动补，
  不配置则回退到模组自带的默认蓝图贴图。
* **结构文件也很宽容**——本版本不认识的方块（可选模组的方块，或者改过名的方块）会变成空气，
  而不是让整张蓝图作废，并会在日志里列出一次具体是哪些。
* **名称与说明既支持翻译键也支持直接写字**，`§` 颜色代码照常生效。
* **联机可用**——结构按需下发给每个客户端，所以即使客户端没有数据包也能看到预览。
* **内置一本写好的说明书**，就放在创造选项卡里，署名作者。
* **内置示例蓝图**（初始小屋），选项卡永远不会是空的。

---

## 运行环境

| Minecraft | 加载器 | Java | 下载文件 |
|---|---|---|---|
| 1.21.1 | NeoForge 21.1+ | 21 | `custom_building-1.21.1-*.jar` |
| 1.20.1 | Forge 47+ | 17 | `custom_building-1.20.1-*.jar` |

预览与建造面板需要客户端和服务端都装。所有的建造行为都以服务端为准：服务端决定放什么，客户端只负责显示。

---

## 数据包格式

### 文件放哪里

```
data/<命名空间>/custom_building/blueprint/<名称>.json     蓝图定义
data/<命名空间>/structure/<名称>.nbt                      结构文件
assets/<命名空间>/textures/...                            预览图与物品贴图
```

蓝图 id 就是 `<命名空间>:<名称>`，例如 `custom_building:initial_house`。同目录下还有一个非 json 后缀的
模板文件 `blueprint_template.txt`（永远不会被解析），复制一份改名成 `.json` 再改内容即可。

### 定义内容

```json
{
    "structure": "custom_building:initial_house",
    "preview":   "assets/custom_building/textures/blueprint/initial_house.png",
    "texture":   "assets/custom_building/textures/item/initial_house.png",
    "name":      "§6初始小屋蓝图",
    "tooltip":   ["§a右键地面来使用"]
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `structure` | 是 | 结构名。写裸名字时按本文件的命名空间补全，也可以写完整 id。 |
| `preview` | 否 | 悬停物品时显示的预览图，会被拉伸成 4:3。 |
| `texture` | 否 | 背包里的物品贴图。不写、留空或写 `default` 时使用模组自带的蓝图贴图。 |
| `name` | 否 | 物品名称——可以填翻译键，也可以直接写字并带 `§` 颜色代码。默认用蓝图 id。 |
| `tooltip` | 否 | 一行字符串或字符串数组，规则同 `name`。 |

贴图路径很宽容，下面几种写法意思完全一样：

```
custom_building:textures/item/initial_house.png            完整资源位置
assets/custom_building/textures/item/initial_house.png     在 jar 里的样子
textures/item/initial_house.png                            相对本文件的命名空间
item/initial_house.png                                     自动补 textures/
item/initial_house                                         自动补 .png
```

### 怎么做出结构文件

在游戏里搭好建筑，用结构方块保存（或任何能写出原版 `structure/*.nbt` 的工具），
把文件丢进 `data/<命名空间>/structure/`。蓝图默认朝南放置，朝向由面板旋转。

---

## 游戏内使用

1. 手持蓝图**右键地面**，打开建造面板。
2. 在左边选**朝向**（北/东/南/西），右边确认预览图。
3. 点 **预览！** 在世界里放下半透明虚影，然后：
   * **左键** 取消；
   * **右键** 建造；
   * **潜行 + 滚轮** 转动朝向。
   也可以直接点 **建造！**。
4. 成功会在聊天栏提示；生存模式每次消耗一个蓝图，创造模式不消耗。

---

## 说明与限制

* **所有蓝图共用一个物品 id**（`custom_building:blueprint`）。Minecraft 的物品注册表在启动后就冻结了，
  一蓝图一物品在技术上不可能；所以每个物品堆自身携带它属于哪张蓝图，页签、名称、tooltip、贴图和
  行为都由它决定。实际使用中页签里就是一件件不同的物品，而且增删蓝图永远不会弄坏已有物品。
* 幽灵预览每个结构上限 6 万个方块。更大的结构仍然可以建造，只是没有虚影。
* 结构的包围盒里如果是空气，那块空间会被清空，和原版结构方块的行为一致。
* 内置示例是纯原版方块的木屋，所以在所有支持的版本上都能完整显示。

---

## 鸣谢与授权

* 模组作者：**Plume Jade**
* 建造面板、面板贴图与放置提示文案参考了 [MC-Prefab](https://github.com/WuestMan/MC-Prefab)（作者 WuestMan，MIT）。
* 幽灵预览的渲染手法参考了 [Litematica](https://github.com/maruohon/litematica)（作者 masa）。
* 允许整合包收录本模组，也允许随意修改内置示例数据包。
